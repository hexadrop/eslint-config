const TYPESCRIPT_GLOBS = ['**/*.?([cm])ts?(x)'];
const DTS_GLOBS = ['**/*.d.ts'];
const TEST_GLOBS = ['**/*.spec.[jt]s?(x)', '**/*.test.[jt]s?(x)'];

export { DTS_GLOBS, TEST_GLOBS, TYPESCRIPT_GLOBS };
