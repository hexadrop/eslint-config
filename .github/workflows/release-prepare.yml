name: "Release: prepare"

on:
  push:
    branches: [ develop ]

jobs:
  check:
    name: Check
    uses: hexadrop/github-workflows/.github/workflows/check.yml@v3
    with:
      commands: '["lint:ci", "typecheck"]'
      cache-commands: '["lint:ci"]'

  release-prepare:
    name: Release
    needs: [ check ]
    uses: hexadrop/github-workflows/.github/workflows/release-prepare.yml@v3
    with:
      base-branch: main
