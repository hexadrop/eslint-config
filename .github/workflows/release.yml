name: "Release"

on:
  push:
    branches: [ main, develop ]

jobs:
  check:
    name: Check
    uses: hexadrop/github-workflows/.github/workflows/check.yml@v3
    with:
      commands: '["lint:ci", "typecheck"]'
      cache-commands: '["lint:ci"]'

  detect-changeset-change:
    name: Changeset
    uses: hexadrop/github-workflows/.github/workflows/detect-changeset-change.yml@v3

  publish:
    name: Publish
    needs: [ check, detect-changeset-change ]
    if: github.ref == 'refs/heads/main' || needs.detect-changeset-change.outputs.should_publish_beta == 'true'
    permissions:
      contents: 'write'
      id-token: 'write'
      issues: 'write'
      pull-requests: 'write'
    uses: hexadrop/github-workflows/.github/workflows/release.yml@v3
    with:
      snapshot: ${{ github.ref == 'refs/heads/develop' }}
      concurrency-group: release-${{ github.ref_name }}
